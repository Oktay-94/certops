// Drizzle schema for CertOps SQLite database.
// Define tables here as features land. Keep types portable so a later
// move to Postgres (Neon/Supabase/RDS) only requires swapping the driver.
export {};
